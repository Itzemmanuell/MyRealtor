# Server-only: never prefix these values with VITE_.
# Create a Resend API key and verify a sender domain in Resend.
RESEND_API_KEY=
CONTACT_FROM_EMAIL=

# Optional public phone number in international format. Leave blank to hide Call.
VITE_CONTACT_PHONE=
